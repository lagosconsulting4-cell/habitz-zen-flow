# 🎨 Diagrama Visual - Fluxo de Deploy

## 📊 Fluxo Completo do Deploy

```
┌─────────────────────────────────────────────────────────────────┐
│  DESENVOLVEDOR                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  1. Edita arquivos:                                       │  │
│  │     - LP: public/*.html, public/*.css, public/*.js       │  │
│  │     - App: app/src/**/*.tsx                              │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  2. git add . && git commit -m "..." && git push         │  │
│  └────────────────────┬─────────────────────────────────────┘  │
└─────────────────────────┼────────────────────────────────────────┘
                          │
                          │ PUSH
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│  GITHUB                                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Repositório: brunofalci00/loteriAI                      │  │
│  │  Branch: master                                           │  │
│  └────────────────────┬─────────────────────────────────────┘  │
└─────────────────────────┼────────────────────────────────────────┘
                          │
                          │ WEBHOOK
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│  VERCEL - Início do Build                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  1. Clone do repositório (5-6 segundos)                  │  │
│  │     git clone github.com/brunofalci00/loteriAI           │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  2. Lê vercel.json NA RAIZ                               │  │
│  │     Prod/vercel.json                                     │  │
│  │                                                           │  │
│  │     {                                                     │  │
│  │       "buildCommand": "cd LP_loteri.AI && ...",          │  │
│  │       "outputDirectory": "LP_loteri.AI/dist"             │  │
│  │     }                                                     │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  3. Entra na pasta LP_loteri.AI/                         │  │
│  │     cd LP_loteri.AI                                      │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  4. Executa: npm install && npm run build                │  │
│  │                                                           │  │
│  │     Isso roda 3 scripts do package.json:                 │  │
│  │                                                           │  │
│  │     a) npm run install                                   │  │
│  │        → cd app && npm install                           │  │
│  │        → Instala deps do React (~1s)                     │  │
│  │                                                           │  │
│  │     b) npm run build:app                                 │  │
│  │        → cd app && npm run build                         │  │
│  │        → Vite builda React (~8s)                         │  │
│  │        → Gera app/dist/                                  │  │
│  │                                                           │  │
│  │     c) npm run build:organize                            │  │
│  │        → node scripts/organize-dist.js                   │  │
│  │        → Organiza dist/ final                            │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  5. Script organize-dist.js executa:                     │  │
│  │                                                           │  │
│  │     • Limpa dist/ anterior                               │  │
│  │     • Copia public/* → dist/ (raiz)                      │  │
│  │     • Copia app/dist/* → dist/app/                       │  │
│  │                                                           │  │
│  │     Resultado:                                            │  │
│  │     dist/                                                │  │
│  │     ├── index.html     ← LP                              │  │
│  │     ├── quiz.html                                        │  │
│  │     ├── styles.css                                       │  │
│  │     └── app/           ← React App                       │  │
│  │         └── index.html                                   │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  6. Vercel pega LP_loteri.AI/dist/ e faz deploy         │  │
│  │     Duração total: ~14-24 segundos                       │  │
│  └────────────────────┬─────────────────────────────────────┘  │
└─────────────────────────┼────────────────────────────────────────┘
                          │
                          │ DEPLOY
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│  PRODUÇÃO - www.fqdigital.com.br                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Rotas servidas:                                          │  │
│  │                                                           │  │
│  │  GET /                  → dist/index.html (LP)           │  │
│  │  GET /quiz              → dist/quiz.html                 │  │
│  │  GET /thanks            → dist/thanks.html               │  │
│  │  GET /styles.css        → dist/styles.css                │  │
│  │  GET /fb-capi.js        → dist/fb-capi.js                │  │
│  │                                                           │  │
│  │  GET /app               → dist/app/index.html (React)    │  │
│  │  GET /app/dashboard     → dist/app/index.html (rewrite)  │  │
│  │  GET /app/auth          → dist/app/index.html (rewrite)  │  │
│  │  GET /app/lottery/*     → dist/app/index.html (rewrite)  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Estrutura de Arquivos Detalhada

```
Prod/
│
├── 📄 vercel.json ⚠️ CRÍTICO
│   └── Diz ao Vercel: "vá para LP_loteri.AI e rode npm run build"
│
├── 📁 LP_loteri.AI/  ← PROJETO PRINCIPAL
│   │
│   ├── 📄 vercel.json
│   │   └── Define rewrites para React Router funcionar
│   │
│   ├── 📄 package.json ⚠️ CRÍTICO
│   │   └── Scripts: build, build:app, build:organize
│   │
│   ├── 📁 public/  ← LANDING PAGE (fonte)
│   │   ├── index.html
│   │   ├── quiz.html
│   │   ├── thanks.html
│   │   ├── styles.css
│   │   ├── fb-capi.js
│   │   └── *.mp4, *.mp3
│   │
│   ├── 📁 app/  ← APLICAÇÃO REACT
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   ├── src/
│   │   │   ├── pages/
│   │   │   ├── components/
│   │   │   └── services/
│   │   └── dist/  ← Build do Vite (gerado)
│   │
│   ├── 📁 scripts/
│   │   └── organize-dist.js ⚠️ CRÍTICO
│   │       └── Junta public/ + app/dist/ → dist/
│   │
│   └── 📁 dist/  ← OUTPUT FINAL (gerado)
│       ├── index.html          (da public/)
│       ├── quiz.html           (da public/)
│       ├── styles.css          (da public/)
│       └── app/                (da app/dist/)
│           ├── index.html
│           └── assets/
│
├── 📁 App/  ⚠️ NÃO É USADO
│   └── Projeto antigo, ignorar
│
└── 📁 Docs/  ← VOCÊ ESTÁ AQUI
    ├── ESTRUTURA-PROJETO.md
    └── DIAGRAMA-DEPLOY.md
```

---

## 🔄 Fluxo de Dados - Build Script

```
package.json: "build"
        │
        ├─→ npm run install
        │        │
        │        └─→ cd app && npm install
        │                 │
        │                 └─→ 📦 node_modules instalado
        │
        ├─→ npm run build:app
        │        │
        │        └─→ cd app && npm run build
        │                 │
        │                 └─→ vite build
        │                      │
        │                      └─→ 📂 app/dist/ criado
        │                           ├── index.html
        │                           ├── assets/
        │                           │   ├── index-ABC123.js
        │                           │   └── index-ABC123.css
        │                           └── favicon.ico
        │
        └─→ npm run build:organize
                 │
                 └─→ node scripts/organize-dist.js
                          │
                          ├─→ 🗑️ Limpa dist/
                          │
                          ├─→ 📋 Copia public/* → dist/
                          │        │
                          │        └─→ dist/index.html
                          │            dist/quiz.html
                          │            dist/styles.css
                          │            dist/fb-capi.js
                          │            dist/*.mp4, *.mp3
                          │
                          └─→ 📋 Copia app/dist/* → dist/app/
                                   │
                                   └─→ dist/app/index.html
                                       dist/app/assets/
                                       dist/app/favicon.ico
```

---

## 🌐 Fluxo de Requisições HTTP

```
Usuário digita: www.fqdigital.com.br/
                        │
                        ▼
        ┌───────────────────────────────┐
        │  DNS resolve para Vercel      │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Vercel recebe requisição     │
        │  Host: www.fqdigital.com.br   │
        │  Path: /                      │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Vercel consulta vercel.json  │
        │  outputDirectory: LP_loteri.AI/dist │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Path = / ?                   │
        │  Serve: dist/index.html       │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Retorna HTML da Landing Page │
        └───────────────────────────────┘


Usuário digita: www.fqdigital.com.br/app/dashboard
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Vercel recebe requisição     │
        │  Path: /app/dashboard         │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Vercel consulta rewrites     │
        │  /app/:path → /app/index.html │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Serve: dist/app/index.html   │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  React Router toma controle   │
        │  Renderiza página Dashboard   │
        └───────────────────────────────┘
```

---

## ⚙️ Configuração em Camadas

```
CAMADA 1: vercel.json (RAIZ)
├─ Define onde está o projeto: LP_loteri.AI/
├─ Define como buildar: npm install && npm run build
└─ Define output: LP_loteri.AI/dist/
        │
        ▼
CAMADA 2: LP_loteri.AI/package.json
├─ Define o que é "npm run build"
│  └─ install + build:app + build:organize
└─ Scripts executados em ordem
        │
        ▼
CAMADA 3: LP_loteri.AI/scripts/organize-dist.js
├─ Junta public/ (LP) + app/dist/ (React)
└─ Cria estrutura final no dist/
        │
        ▼
CAMADA 4: LP_loteri.AI/vercel.json
├─ Define rewrites para /app/*
└─ Define headers de cache
        │
        ▼
RESULTADO: dist/ servido pelo Vercel
```

---

## 🎯 Pontos Críticos de Falha

```
❌ SE deletar vercel.json (raiz)
    ↓
    Vercel não sabe como buildar
    ↓
    Build de 900ms apenas
    ↓
    404 em tudo

❌ SE deletar organize-dist.js
    ↓
    dist/ não é criado corretamente
    ↓
    LP e App não ficam juntos
    ↓
    404 ou estrutura quebrada

❌ SE modificar Root Directory errado
    ↓
    Vercel procura arquivos no lugar errado
    ↓
    Build falha ou serve conteúdo errado

❌ SE remover rewrites do vercel.json
    ↓
    /app/dashboard, /app/auth dão 404
    ↓
    React Router não funciona
```

---

## ✅ Checklist de Sanidade

Antes de fazer qualquer mudança crítica, verifique:

```
□ vercel.json existe na raiz do repo
□ LP_loteri.AI/vercel.json existe
□ LP_loteri.AI/package.json tem script "build"
□ LP_loteri.AI/scripts/organize-dist.js existe
□ Root Directory no Vercel = LP_loteri.AI
□ Último deploy demorou > 10 segundos
□ Logs mostram "Build organizado com sucesso!"
□ www.fqdigital.com.br/ abre a LP
□ www.fqdigital.com.br/app abre o React app
```

Se algum item estiver ❌, investigar antes de prosseguir!

---

**FIM DO DIAGRAMA**
